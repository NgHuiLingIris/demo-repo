name: Release Workflow

on:
  push:
    branches:
      - main
      - master
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Semantic Release
        uses: cycjimmy/semantic-release-action@v4
        env:
          PERSONAL_ACCESS_TOKEN: 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
